# markweave.cloud 落地页
server {
    listen 80;
    server_name markweave.cloud www.markweave.cloud;

    location / {
        proxy_pass http://127.0.0.1:13000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# auth.markweave.cloud 认证服务
server {
    listen 80;
    server_name auth.markweave.cloud;

    location / {
        proxy_pass http://127.0.0.1:13230;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# lic.markweave.cloud 授权服务
server {
    listen 80;
    server_name lic.markweave.cloud;

    location / {
        proxy_pass http://127.0.0.1:13210;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# pay.markweave.cloud 支付服务
server {
    listen 80;
    server_name pay.markweave.cloud;

    location / {
        proxy_pass http://127.0.0.1:13220;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
